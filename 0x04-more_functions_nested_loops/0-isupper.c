#include "main.h"

/**
 * _isupper - uppercase letters.
 * @c: char that would be checked.
 * Return: 1 if character is uppercase, 0 otherwise.
 */

int _isupper(int c)
{
	if (c >= 'A' && C <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
