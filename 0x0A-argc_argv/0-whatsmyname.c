#include <stdio.h>
#include "main.h"
/**
 * main - function that prints name of the programme.
 * @argc: number of arguements.
 * @argv: array of arguments.
 * Return: 0 (Successful).
 */
int main(int argc, char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
