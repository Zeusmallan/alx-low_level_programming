#include "main.h"
/**
 * _abs - Prints the absolute value of an integer.
 * @int: Integer or number.
 * Return: absolute value.
 */
int _abs(int);
{
	if (i >= 0)
	{
		return (1);
	}
	else
	{
		return (i * -1);
	}
}
