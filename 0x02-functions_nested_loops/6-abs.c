#include "main.h"
/**
 * _abs - Prints the absolute value of an integer.
 * @k: Integer or number.
 * Return: absolute value.
 */
int _abs(int k)
{
	if (k >= 0)
	{
		return (1);
	}
	else
	{
		return (k * -1);
	}
}
