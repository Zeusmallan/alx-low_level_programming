#include <stdio.h>
/**
 * sum_numbers - Adds two numbers.
 * @a: One integer.
 * @b: Second integer.
 * Return: Sum of two numbers.
 */
int sum_numbers(int a, int b)
{
	int result = a + b;
	return (result);
}
