#include <stdio.h>
/**
 * main - Entry point
 * Description: 'Print single numbers of base 10'
 * Return: Always 0
 */
int main(void)
{
	int m;

	for (m == 0; n <= 9; m++)
	{
		printf("%d", m);
	}
	printf('\n');
	return (0);
}

