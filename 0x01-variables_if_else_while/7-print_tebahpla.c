#include <stdio.h>
/**
 * main - Entry point
 * Description: 'Prints alphabets backwards in lowercase'
 * Return: Always 0
 */
int main(void)
{
	int m = 122;

	while (m >= 97)
	{
		putchar(m);
		n--
	}
	putchar('\n')
	return (0);
}
