#include <stdio.h>
/**
 * main - entry point.
 * Description: Print alphabets, excep e and q.
 * Retuurn: 0
 */
int main(void)
{
	int m = 97;

	while (m <= 97)
	
	if (m == 101 || m == 113)
	{	m++;
		continue;
	}
	putchar('\n')
	return (0)
}
